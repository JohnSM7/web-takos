---
import '../styles/global.css';
import { negocio, locales } from '@data/business';

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const {
  title = `${negocio.nombre} · Auténticos tacos al pastor en Madrid Centro`,
  description = '🌮 La taquería mexicana más famosa de Madrid. Tacos al pastor marinados 12 horas en achiote y cortados del trompo a la vista. 4,5★ con +13.500 reseñas en Google. C/ Salud 13. Sin reservas.',
  image = '/og-takos-v2.png',
} = Astro.props;

const canonical = new URL(Astro.url.pathname, Astro.site).toString();
// Convierte rutas relativas a URL absoluta (obligatorio para OG / Twitter Cards)
const absoluteImage = image.startsWith('http')
  ? image
  : new URL(image, Astro.site).toString();

const keywords = [
  'tacos al pastor',
  'taquería Madrid',
  'comida mexicana Madrid',
  'Takos al Pastor',
  'tacos 1 euro Madrid',
  'restaurante mexicano centro Madrid',
  'mejores tacos Madrid',
  'Calle Salud 13',
  'Gran Vía mexicano',
].join(', ');

// JSON-LD Restaurant Schema — mapea datos reales del negocio
const localPrincipal = locales.find((l) => l.destacado) ?? locales[0];

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Restaurant',
  name: negocio.nombre,
  description: negocio.descripcion,
  url: canonical,
  telephone: negocio.telefono,
  email: negocio.email,
  image: [absoluteImage, new URL('/images/logo-takos.png', Astro.site).toString()],
  priceRange: '€',
  servesCuisine: ['Mexican', 'Tacos', 'Street Food'],
  acceptsReservations: 'False',
  address: {
    '@type': 'PostalAddress',
    streetAddress: localPrincipal.direccion,
    addressLocality: localPrincipal.ciudad,
    postalCode: localPrincipal.cp,
    addressCountry: 'ES',
  },
  geo: {
    '@type': 'GeoCoordinates',
    latitude: localPrincipal.lat,
    longitude: localPrincipal.lng,
  },
  openingHoursSpecification: [
    {
      '@type': 'OpeningHoursSpecification',
      dayOfWeek: ['Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'],
      opens: '13:00',
      closes: '24:00',
    },
  ],
  aggregateRating: {
    '@type': 'AggregateRating',
    ratingValue: negocio.rating,
    reviewCount: negocio.resenasTotales,
    bestRating: 5,
    worstRating: 1,
  },
  sameAs: [negocio.redes.instagram, negocio.redes.facebook, negocio.redes.tripadvisor],
};
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonical} />

    <!-- Primary Meta -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="keywords" content={keywords} />
    <meta name="theme-color" content="#E87A2C" />
    <meta name="author" content={negocio.nombre} />
    <meta name="robots" content="index, follow, max-image-preview:large" />

    <!-- Open Graph (Facebook, LinkedIn, WhatsApp, Telegram) -->
    <meta property="og:type" content="restaurant.restaurant" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={absoluteImage} />
    <meta property="og:image:secure_url" content={absoluteImage} />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:width" content="1380" />
    <meta property="og:image:height" content="1380" />
    <meta property="og:image:alt" content={`Logo oficial ${negocio.nombre}`} />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content="es_ES" />
    <meta property="og:site_name" content={negocio.nombre} />
    <meta property="restaurant:contact_info:street_address" content={localPrincipal.direccion} />
    <meta property="restaurant:contact_info:locality" content={localPrincipal.ciudad} />
    <meta property="restaurant:contact_info:country_name" content={localPrincipal.pais} />
    <meta property="restaurant:contact_info:postal_code" content={localPrincipal.cp} />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={absoluteImage} />
    <meta name="twitter:image:alt" content={`Logo oficial ${negocio.nombre}`} />
    <meta name="twitter:site" content="@takosalpastor1_oficial" />

    <!-- Geo -->
    <meta name="geo.region" content="ES-MD" />
    <meta name="geo.placename" content="Madrid" />
    <meta
      name="geo.position"
      content={`${localPrincipal.lat};${localPrincipal.lng}`}
    />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Archivo+Black&family=Caveat:wght@500;700&family=Fraunces:ital,opsz,wght@0,9..144,400..900;1,9..144,400..900&family=Space+Mono:wght@400;700&display=swap"
    />

    <!-- Leaflet CSS -->
    <link
      rel="stylesheet"
      href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
      integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
      crossorigin=""
    />

    <!-- JSON-LD -->
    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />

    <!-- SVG filters reutilizables -->
    <slot name="head" />
  </head>
  <body class="min-h-screen font-body">
    <!-- SVG filter para tipografía líquida -->
    <svg width="0" height="0" style="position:absolute" aria-hidden="true">
      <defs>
        <filter id="liquid-distort">
          <feTurbulence
            type="fractalNoise"
            baseFrequency="0.012 0.018"
            numOctaves="2"
            seed="3"
          >
            <animate
              attributeName="baseFrequency"
              dur="14s"
              values="0.012 0.018; 0.018 0.012; 0.012 0.018"
              repeatCount="indefinite"
            />
          </feTurbulence>
          <feDisplacementMap in="SourceGraphic" scale="8" />
        </filter>
      </defs>
    </svg>

    <slot />

    <script>
      // Theming dinámico de fondo según sección visible (Intersection Observer)
      const sections = document.querySelectorAll<HTMLElement>('[data-bg]');
      if ('IntersectionObserver' in window && sections.length) {
        const root = document.documentElement;
        const obs = new IntersectionObserver(
          (entries) => {
            entries.forEach((e) => {
              if (e.isIntersecting && e.intersectionRatio > 0.4) {
                const bg = e.target.getAttribute('data-bg');
                const fg = e.target.getAttribute('data-fg');
                if (bg) root.style.setProperty('--bg-now', bg);
                if (fg) root.style.setProperty('--fg-now', fg);
              }
            });
          },
          { threshold: [0.4, 0.6] }
        );
        sections.forEach((s) => obs.observe(s));
      }

      // Cursor magnético en elementos con [data-magnetic]
      const magnetics = document.querySelectorAll<HTMLElement>('[data-magnetic]');
      magnetics.forEach((el) => {
        el.addEventListener('mousemove', (ev) => {
          const e = ev as MouseEvent;
          const rect = el.getBoundingClientRect();
          const x = e.clientX - rect.left - rect.width / 2;
          const y = e.clientY - rect.top - rect.height / 2;
          el.style.transform = `translate(${x * 0.18}px, ${y * 0.18}px)`;
        });
        el.addEventListener('mouseleave', () => {
          el.style.transform = 'translate(0,0)';
        });
      });

      // Bento spotlight — mouse position en cards
      const cards = document.querySelectorAll<HTMLElement>('.bento-card');
      cards.forEach((c) => {
        c.addEventListener('mousemove', (ev) => {
          const e = ev as MouseEvent;
          const r = c.getBoundingClientRect();
          c.style.setProperty('--mx', `${e.clientX - r.left}px`);
          c.style.setProperty('--my', `${e.clientY - r.top}px`);
        });
      });
    </script>
  </body>
</html>
